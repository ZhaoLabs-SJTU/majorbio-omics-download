# 🌟 新手完全指南 — majorbio-omics-download

> 从零开始，把美吉云（Majorbio）项目中心的组学数据批量下载到本地/服务器，并生成 scRNA 质控报告。

## 适用人群
- 第一次接触美吉云数据下载的组学分析小白
- 需要批量下载大量样本、不想逐个手动点的用户

## 你需要准备
- 美吉云账号（手机号/邮箱 + 密码）
- Linux / macOS（Windows 用 WSL）
- Python 3.8+

## 第一章 安装依赖（约 5 分钟）

```bash
pip install requests ddddocr pandas matplotlib python-docx openpyxl python-pptx
```

## 第二章 下载数据（约 10 分钟）

```bash
git clone https://github.com/ZhaoLabs-SJTU/majorbio-omics-download.git
cd majorbio-omics-download

python scripts/majorbio_download.py \
  --account 你的账号 \
  --password 你的密码 \
  --outdir ./data
```

> 🔐 凭据也可用环境变量 `MAJORBIO_ACCOUNT` / `MAJORBIO_PASSWORD` 传入，避免写进命令行历史。

脚本会自动：登录（含验证码 OCR）→ 遍历任务/文件 → 逐文件下载（断点续传）。

## 第三章 生成质控报告（约 5 分钟）

```bash
python scripts/scRNA_qc_report.py \
  --datadir ./data \
  --outdir ./report \
  --prefix 我的项目
```

会自动生成 Word / Excel / PPT 三份质控报告 + 4 张统计图。

## 第四章 常见问题

| 现象 | 解决 |
|------|------|
| 下载直链报 `400 InvalidArgument` | 预签名 URL 已自带签名，去掉 Authorization 头 |
| 报 `12002 文件已上锁` | 到项目中心 UI 解锁该文件后重试 |
| 验证码识别失败 | 脚本自动循环重试（ddddocr 命中率约 1/6） |
| 图表中文豆腐块 | 注册中文字体，见 SKILL.md「图表中文」 |

## 验收清单
- [ ] 文件数与平台显示一致
- [ ] 无 `.part` 残留文件
- [ ] 所有 `.gz` 通过 `gzip -t`
- [ ] 抽查 1~2 个文件 MD5 与重下一致

> 📖 完整流程、故障排查与速查表见 [SKILL.md](SKILL.md)；Word/Excel/PPT 三件套见 [小白文档](小白文档/)。
